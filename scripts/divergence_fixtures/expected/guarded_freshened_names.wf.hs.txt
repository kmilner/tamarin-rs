WARNING: the following wellformedness checks failed!

 Formula guardedness
====================

  Lemma `Shadow' cannot be converted to a guarded formula:
    unguarded variable(s) 'x.1', 'z' in the subformula
      "∀ x z. (<x, z> = x) ⇒ (⊥)"
    in the formula
      "∀ x #NOW. (Foo( x ) @ #NOW) ⇒ (∀ x.1 z. (<x.1, z> = x.1) ⇒ (⊥))"
  
  Lemma `GuardedFirst' cannot be converted to a guarded formula:
    unguarded variable(s) 'x.1', 'z' in the subformula
      "∀ x z. (<x, z> = x) ⇒ (⊥)"
    in the formula
      "(∀ x #i. (Foo( x ) @ #i) ⇒ (⊥)) ∧ (∀ x z. (<x, z> = x) ⇒ (⊥))"
  
  Lemma `UnguardedFirst' cannot be converted to a guarded formula:
    unguarded variable(s) 'x', 'z' in the subformula
      "∀ x z. (<x, z> = x) ⇒ (⊥)"
    in the formula
      "(∀ x z. (<x, z> = x) ⇒ (⊥)) ∧ (∀ x #i. (Foo( x ) @ #i) ⇒ (⊥))"
*/
