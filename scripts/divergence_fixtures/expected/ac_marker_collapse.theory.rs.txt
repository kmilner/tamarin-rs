theory MarkerNorm

begin

// Function signature and definition of the equational theory E

builtins: multiset
functions: fst/1, pair/2, snd/1, tamXCAbar/1
equations: fst(<x.1, x.2>) = x.1, snd(<x.1, x.2>) = x.2

rule (modulo E) Gen:
   [ In( a ), In( y ) ] --[ D( ) ]-> [ Out( (y++fst(<tamXCAbar(a), y>)) ) ]

  /*
  rule (modulo AC) Gen:
     [ In( a ), In( y ) ] --[ D( ) ]-> [ Out( (y++tamXCAbar(a)) ) ]
  */

lemma triv:
  exists-trace "∃ #i. D( ) @ #i"
/*
guarded formula characterizing all satisfying traces:
"∃ #i. (D( ) @ #i)"
*/
by sorry



end
