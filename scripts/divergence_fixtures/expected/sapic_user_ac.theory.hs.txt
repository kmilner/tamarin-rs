theory SapicUserAcRestr

begin

// Function signature and definition of the equational theory E

builtins: diffie-hellman
functions: fst/1, pair/2, snd/1, add/2 [AC]
equations: fst(<x.1, x.2>) = x.1, snd(<x.1, x.2>) = x.2

heuristic: p

predicate: Eq( a, b )<=>a = b

rule (modulo E) Init[color=#ffffff, process="in(k.1);", issapicrule,
                     role='Process']:
   [ ] --[ Init( ) ]-> [ State_( ) ]

  /* has exactly the trivial AC variant */

rule (modulo E) ink_0_[color=#ffffff, process="in(k.1);", issapicrule,
                       role='Process']:
   [ State_( ), In( k.1 ) ] --> [ State_1( k.1 ) ]

  /*
  rule (modulo AC) ink_0_[color=#ffffff, process="in(k.1);", issapicrule,
                          role='Process']:
     [ State_( ), In( k ) ] --> [ State_1( k ) ]
  */

rule (modulo E) letuvgkaaddbaddk_0_1[color=#ffffff,
                                     process="let <u.1, v.1>=<'g'^k.1, ('a' add 'b' add k.1)>", issapicrule,
                                     role='Process']:
   [ State_1( k.1 ) ]
  -->
   [ Let_11( <'g'^k.1, ('a' add 'b' add k.1)>, k.1 ) ]

  /*
  rule (modulo AC) letuvgkaaddbaddk_0_1[color=#ffffff,
                                        process="let <u.1, v.1>=<'g'^k.1, ('a' add 'b' add k.1)>",
                                        issapicrule, role='Process']:
     [ State_1( k ) ] --> [ Let_11( <z, ('a' add 'b' add k)>, k ) ]
    variants (modulo AC)
    1. k     = k.5
       z     = 'g'^k.5
    
    2. k     = one
       z     = 'g'
  */

rule (modulo E) letuvgkaaddbaddk_1_1[color=#ffffff,
                                     process="let <u.1, v.1>=<'g'^k.1, ('a' add 'b' add k.1)>", issapicrule,
                                     role='Process']:
   [ Let_11( <u.1, v.1>, k.1 ) ] --> [ State_11( k.1, u.1, v.1 ) ]

  /*
  rule (modulo AC) letuvgkaaddbaddk_1_1[color=#ffffff,
                                        process="let <u.1, v.1>=<'g'^k.1, ('a' add 'b' add k.1)>",
                                        issapicrule, role='Process']:
     [ Let_11( <u, v>, k ) ] --> [ State_11( k, u, v ) ]
  */

restriction Restr_letuvgkaaddbaddk_2_1_1:
  "∀ x #NOW.
    (Restr_letuvgkaaddbaddk_2_1_1( x ) @ #NOW) ⇒ (∀ u v. (<u, v> = x) ⇒ (⊥))"
  // safety formula

  /*
  expanded formula:
  "∀ x #NOW.
    (Restr_letuvgkaaddbaddk_2_1_1( x ) @ #NOW) ⇒ (∀ u v. (<u, v> = x) ⇒ (⊥))"
  */

rule (modulo E) letuvgkaaddbaddk_2_1[color=#ffffff,
                                     process="let <u.1, v.1>=<'g'^k.1, ('a' add 'b' add k.1)>", issapicrule,
                                     role='Process']:
   [ Let_11( <'g'^k.1, ('a' add 'b' add k.1)>, k.1 ) ]
  --[ Restr_letuvgkaaddbaddk_2_1_1( <'g'^k.1, ('a' add 'b' add k.1)> ) ]->
   [ State_12( k.1 ) ]

  /*
  rule (modulo AC) letuvgkaaddbaddk_2_1[color=#ffffff,
                                        process="let <u.1, v.1>=<'g'^k.1, ('a' add 'b' add k.1)>",
                                        issapicrule, role='Process']:
     [ Let_11( <z, ('a' add 'b' add k)>, k ) ]
    --[ Restr_letuvgkaaddbaddk_2_1_1( <z, ('a' add 'b' add k)> ) ]->
     [ State_12( k ) ]
    variants (modulo AC)
    1. k     = k.5
       z     = 'g'^k.5
    
    2. k     = one
       z     = 'g'
  */

rule (modulo E) outuv_0_11[color=#ffffff, process="out(<u.1, v.1>);",
                           issapicrule, role='Process']:
   [ State_11( k.1, u.1, v.1 ) ]
  -->
   [ State_111( k.1, u.1, v.1 ), Out( <u.1, v.1> ) ]

  /*
  rule (modulo AC) outuv_0_11[color=#ffffff, process="out(<u.1, v.1>);",
                              issapicrule, role='Process']:
     [ State_11( k, u, v ) ] --> [ State_111( k, u, v ), Out( <u, v> ) ]
  */

rule (modulo E) p_0_111[color=#ffffff, process="0", issapicrule,
                        role='Process']:
   [ State_111( k.1, u.1, v.1 ) ] --> [ ]

  /*
  rule (modulo AC) p_0_111[color=#ffffff, process="0", issapicrule,
                           role='Process']:
     [ State_111( k, u, v ) ] --> [ ]
  */

restriction Restr_ifEqgkaaddcaddkk_0_12_1:
  "∀ x #NOW x.1.
    (Restr_ifEqgkaaddcaddkk_0_12_1( x, x.1 ) @ #NOW) ⇒ (x = x.1)"
  // safety formula

  /*
  expanded formula:
  "∀ x #NOW x.1.
    (Restr_ifEqgkaaddcaddkk_0_12_1( x, x.1 ) @ #NOW) ⇒ (x = x.1)"
  */

rule (modulo E) ifEqgkaaddcaddkk_0_12[color=#ffffff,
                                      process="if Eq( <'g'^k.1, ('a' add 'c' add k.1)>, k.1 )", issapicrule,
                                      role='Process']:
   [ State_12( k.1 ) ]
  --[
  Restr_ifEqgkaaddcaddkk_0_12_1( <'g'^k.1, ('a' add 'c' add k.1)>, k.1 )
  ]->
   [ State_121( k.1 ) ]

  /*
  rule (modulo AC) ifEqgkaaddcaddkk_0_12[color=#ffffff,
                                         process="if Eq( <'g'^k.1, ('a' add 'c' add k.1)>, k.1 )",
                                         issapicrule, role='Process']:
     [ State_12( k ) ]
    --[ Restr_ifEqgkaaddcaddkk_0_12_1( <z, ('a' add 'c' add k)>, k ) ]->
     [ State_121( k ) ]
    variants (modulo AC)
    1. k     = k.5
       z     = 'g'^k.5
    
    2. k     = one
       z     = 'g'
  */

restriction Restr_ifEqgkaaddcaddkk_1_12_1:
  "∀ x #NOW x.1.
    (Restr_ifEqgkaaddcaddkk_1_12_1( x, x.1 ) @ #NOW) ⇒ (¬(x = x.1))"
  // safety formula

  /*
  expanded formula:
  "∀ x #NOW x.1.
    (Restr_ifEqgkaaddcaddkk_1_12_1( x, x.1 ) @ #NOW) ⇒ (¬(x = x.1))"
  */

rule (modulo E) ifEqgkaaddcaddkk_1_12[color=#ffffff,
                                      process="if Eq( <'g'^k.1, ('a' add 'c' add k.1)>, k.1 )", issapicrule,
                                      role='Process']:
   [ State_12( k.1 ) ]
  --[
  Restr_ifEqgkaaddcaddkk_1_12_1( <'g'^k.1, ('a' add 'c' add k.1)>, k.1 )
  ]->
   [ State_122( k.1 ) ]

  /*
  rule (modulo AC) ifEqgkaaddcaddkk_1_12[color=#ffffff,
                                         process="if Eq( <'g'^k.1, ('a' add 'c' add k.1)>, k.1 )",
                                         issapicrule, role='Process']:
     [ State_12( k ) ]
    --[ Restr_ifEqgkaaddcaddkk_1_12_1( <z, ('a' add 'c' add k)>, k ) ]->
     [ State_122( k ) ]
    variants (modulo AC)
    1. k     = k.5
       z     = 'g'^k.5
    
    2. k     = one
       z     = 'g'
  */

rule (modulo E) outyes_0_121[color=#ffffff, process="out('yes');",
                             issapicrule, role='Process']:
   [ State_121( k.1 ) ] --> [ State_1211( k.1 ), Out( 'yes' ) ]

  /*
  rule (modulo AC) outyes_0_121[color=#ffffff, process="out('yes');",
                                issapicrule, role='Process']:
     [ State_121( k ) ] --> [ State_1211( k ), Out( 'yes' ) ]
  */

rule (modulo E) p_0_1211[color=#ffffff, process="0", issapicrule,
                         role='Process']:
   [ State_1211( k.1 ) ] --> [ ]

  /*
  rule (modulo AC) p_0_1211[color=#ffffff, process="0", issapicrule,
                            role='Process']:
     [ State_1211( k ) ] --> [ ]
  */

rule (modulo E) outno_0_122[color=#ffffff, process="out('no');",
                            issapicrule, role='Process']:
   [ State_122( k.1 ) ] --> [ State_1221( k.1 ), Out( 'no' ) ]

  /*
  rule (modulo AC) outno_0_122[color=#ffffff, process="out('no');",
                               issapicrule, role='Process']:
     [ State_122( k ) ] --> [ State_1221( k ), Out( 'no' ) ]
  */

rule (modulo E) p_0_1221[color=#ffffff, process="0", issapicrule,
                         role='Process']:
   [ State_1221( k.1 ) ] --> [ ]

  /*
  rule (modulo AC) p_0_1221[color=#ffffff, process="0", issapicrule,
                            role='Process']:
     [ State_1221( k ) ] --> [ ]
  */

restriction single_session:
  "∀ #i #j. ((Init( ) @ #i) ∧ (Init( ) @ #j)) ⇒ (#i = #j)"
  // safety formula

  /*
  expanded formula:
  "∀ #i #j. ((Init( ) @ #i) ∧ (Init( ) @ #j)) ⇒ (#i = #j)"
  */



end
