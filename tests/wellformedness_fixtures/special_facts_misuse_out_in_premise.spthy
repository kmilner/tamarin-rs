// Fixture: triggers `Fact usage`.
// `Out` (adversary output) is a conclusion-only fact; it must not
// appear in the LHS of a rule.
theory SpecialFactsOut
begin

rule Bad:
   [ Out(x) ]
  --[ ]->
   [ ]

end
